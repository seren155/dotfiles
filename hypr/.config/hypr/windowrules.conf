##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hypr.land/Configuring/Window-Rules/ for more
# See https://wiki.hypr.land/Configuring/Workspace-Rules/ for workspace rules

# Example windowrules that are useful

windowrule {
    # Ignore maximize requests from all apps. You'll probably like this.
    name = suppress-maximize-events
    match:class = .*

    suppress_event = maximize
}

windowrule {
    # Fix some dragging issues with XWayland
    name = fix-xwayland-drags
    match:class = ^$
    match:title = ^$
    match:xwayland = true
    match:float = true
    match:fullscreen = false
    match:pin = false

    no_focus = true
}

# Hyprland-run windowrule
windowrule {
    name = move-hyprland-run

    match:class = hyprland-run

    move = 20 monitor_h-120
    float = yes
}

# 1. Definir el comportamiento del espacio de trabajo cuando hay una ventana sola
workspace = w[t1], gapsin:0, gapsout:0, border:false, decorate:false

# 2. Regla de ventana para forzar el redondeo a cero y quitar bordes
windowrule {
  name = windowrule-1
  rounding = 0
  border_color = rgba(00000000)
  match:workspace = w[t1]
}

#workspace = w[t1], gapsout:0, gapsin:0
#windowrulev2 = bordercolor rgba(00000000), onworkspace:w[t1], rounding=0, border=false
